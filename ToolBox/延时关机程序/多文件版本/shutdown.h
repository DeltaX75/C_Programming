//Shutdown.h
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <windows.h>

#ifndef _SHUTDOWN_
#define _SHUTDOWN_

void UI();
void Shutdown(char i[]);
int char_int(char i[]);
float char_float(char i[]);
int i_or_f(char in[]);
void Message1();
void int_to_char(int in,char x[]);

#endif

